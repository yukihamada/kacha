import SwiftUI

enum KAGITheme {
    static let appName = "KAGI"
    static let tagline = "開いた、ウェルカム。"

    // Brand colors (updated, more refined)
    static let gold = Color(hex: "E8A838")
    static let accent = Color(hex: "3B9FE8")
    static let success = Color(hex: "10B981")
    static let warn = Color(hex: "F59E0B")
    static let danger = Color(hex: "EF4444")
    static let bg = Color(hex: "06060E")     // slightly bluer black
    static let card = Color(white: 1, opacity: 0.05)
    static let cardBorder = Color(white: 1, opacity: 0.08)
}
